
version: 2

models:
  - name: content_movie_gold
    description: "All movies"
    columns:
      - name: id
        description: "alphanumeric unique identifier of the movie"
        tests:
          - not_null
          - unique
          - relationships:
              to: ref('content_gold')
              field: id
      - name: parent_id
        description: "The movie series"
        tests:
          - relationships:
              to: ref('content_movieseries_gold')
              field: id