
version: 2

models:
  - name: content_movieseries_gold
    description: "All movie series"
    columns:
      - name: id
        description: "alphanumeric unique identifier of the movie series"
        tests:
          - not_null
          - unique
          - relationships:
              to: ref('content_gold')
              field: id